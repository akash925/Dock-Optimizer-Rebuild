# Dock Optimizer Environment Configuration
# 
# 🔒 SECURITY NOTICE:
# - In production, sensitive values are managed by Doppler
# - This file contains safe defaults for local development
# - Never commit actual secrets to this file

# ===================
# CRITICAL (Required) - Managed by Doppler in Production
# ===================

# Database (Neon PostgreSQL)
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# Email Service (SendGrid) 
SENDGRID_API_KEY="SG.your-sendgrid-api-key"
SENDGRID_FROM_EMAIL="noreply@yourdomain.com"

# Security Secrets
SESSION_SECRET="your-session-secret"
JWT_SECRET="your-jwt-secret"
JWT_PRIVATE_KEY="your-jwt-private-key"

# ===================
# AWS S3 (Optional) - Graceful fallback to local storage
# ===================

AWS_S3_BUCKET="dock-optimizer-prod"
AWS_ACCESS_KEY_ID="your-access-key-id"
AWS_SECRET_ACCESS_KEY="your-secret-access-key"
AWS_REGION="us-east-1"

# ===================
# Application Settings (Safe to commit)
# ===================

NODE_ENV="development"
PORT="5001"
HOST_URL="http://localhost:5001"

# ===================
# Redis (Optional) - Managed by Doppler in Production
# ===================

REDIS_URL="redis://localhost:6379"

# ===================
# Feature Flags (Safe to commit)
# ===================

ENABLE_EMAIL_NOTIFICATIONS="true"
ENABLE_ASSET_MANAGER="true"
ENABLED_MODULES="companyAssets,calendar,analytics,bookingPages,emailNotifications"
ENFORCE_WEEKEND_RULE="true"
